package com.example.myapplication.widget;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.LinearLayout;

import androidx.annotation.Nullable;

public class ExitDialog extends LinearLayout {
    public ExitDialog(Context context) {
        super(context);
    }

    public ExitDialog(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }

    public ExitDialog(Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }

    public ExitDialog(Context context, AttributeSet attrs, int defStyleAttr, int defStyleRes) {
        super(context, attrs, defStyleAttr, defStyleRes);
    }
}
